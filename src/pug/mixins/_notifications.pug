mixin notification__error(text)
    div.notification__error #{text}

mixin notification__error--text(text)
    div.notification__error--text
        div.notification__error.notification__error--top #{text}
        p.mb-25 Используйте другой email чтобы создать новый аккаунт
        p.m-0 Или воспользуйтесь #[a(href="#!") восстановлением пароля], чтобы войти на сайт.

mixin notification__success(text)
    div.notification__success #{text}

mixin notification__notify(text)
    div.notification__notify #{text}